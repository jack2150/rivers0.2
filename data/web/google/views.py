from django.shortcuts import render

# Create your views here.
# todo: use panda get google stock price then insert into db with source=google
def google_get_stock_select_view(request):
    return

def google_get_stock_run_view(request, symbol=''):
    return